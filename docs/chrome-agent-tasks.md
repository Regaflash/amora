# פקודות ל-Claude in Chrome

משימות שדורשות ממשק דפדפן שאין לי גישה אליו — Meta Events Manager, אוריגמי,
Google Search Console, Google Business Profile. כל בלוק להעתקה כמו שהוא.

**כלל בטיחות שחל על כולן:** Claude in Chrome פועל בדפדפן שלך עם ההרשאות שלך.
אל תיתן לו לאשר תשלום, למחוק, או לשנות תקציב קמפיין. אם פקודה מבקשת "לאשר" —
תעצור ותאשר בעצמך.

---

## 0 · ~~ביטול האסימון שנחשף~~ — בוצע 5.8.2026 ✓

הבעלים ביטל את האסימון הישן והחדש (`META_CAPI_TOKEN`) יושב ב-Supabase Secrets.

---

## הפקודה המלאה — להעתיק כבלוק אחד

זהו הרצף המומלץ. שלושה שלבים, כל אחד נעצר ומדווח לפני שהבא מתחיל.

```
אתה עוזר לי בתפעול העסק שלי — Amora Studio, סטודיו לצילום ווידאו לחתונות
בישראל. האתר: https://www.amora-studios.com

יש לי שלוש משימות בדפדפן. בצע אותן לפי הסדר. אחרי כל שלב — עצור, דווח לי מה
קרה, וחכה לאישור שלי לפני שאתה ממשיך לשלב הבא.

חוקים שחלים על כל השלבים:
- אל תמחק שום דבר. אל תשנה שום שדה, רשומה או הגדרה קיימים. רק להוסיף.
- אל תאשר שום תשלום, שדרוג חבילה או שינוי תקציב. אם מסך כזה מופיע — עצור ושאל.
- אל תמציא נתונים. אם טופס דורש מידע שאין לך — עצור ושאל אותי.
- אם משהו נראה שונה ממה שתיארתי — עצור ותאר לי מה אתה רואה.

═══════════════════════════════════════════
שלב 1 — שדה חדש באוריגמי (הכי חשוב)
═══════════════════════════════════════════

היכנס למערכת אוריגמי שלי.

מצא את סוג הישות שבו נשמרים הלידים — זו הישות שמכילה שדות של "שם מלא" ו"מספר
טלפון" של פניות נכנסות. המזהה הפנימי שלה הוא e_89.

הוסף לישות הזו שדה חדש:
  שם: מזהה ליד Meta
  סוג: טקסט (שורה אחת)
  חובה: לא
  אורך מקסימלי: 60

אחרי שיצרת אותו, מצא את המזהה הפנימי של השדה. הוא בפורמט fld_ ואחריו מספר,
למשל fld_1509. אם הוא לא מוצג בממשק — חפש אותו בכתובת ה-URL של עמוד עריכת
השדה, או ב-HTML של העמוד (לחיצה ימנית → Inspect).

דווח לי: המזהה המדויק של השדה החדש. זה הפלט היחיד שאני צריך מהשלב הזה.
עצור כאן.

═══════════════════════════════════════════
שלב 2 — אימות Search Console לדומיין
═══════════════════════════════════════════

פתח את https://search.google.com/search-console

הוסף נכס חדש. בחר את הסוג "Domain" (הקופסה השמאלית) — לא "URL prefix".
הזן: amora-studios.com

גוגל תציג רשומת TXT לאימות. העתק את הערך המלא שלה במדויק ודווח לי אותו.

עכשיו פתח לשונית חדשה: https://vercel.com
היכנס לפרויקט amora ← Settings ← Domains ← amora-studios.com ← DNS Records.

הוסף רשומה חדשה:
  Type: TXT
  Name: @   (או ריק, אם Vercel לא מקבל @)
  Value: הערך שקיבלת מגוגל

חשוב: אל תיגע בשום רשומה קיימת. במיוחד לא ברשומות מסוג A, CNAME או MX —
שינוי שלהן יוריד את האתר או את המייל.

חזור ל-Search Console ולחץ Verify. אם נכשל — חכה 5 דקות ונסה שוב, הפצת DNS
לוקחת זמן.

דווח לי: האם האימות הצליח.
עצור כאן.

═══════════════════════════════════════════
שלב 3 — פרופיל עסקי בגוגל
═══════════════════════════════════════════

פתח את https://business.google.com וצור פרופיל עסקי חדש.

  שם העסק: Amora Studio
  קטגוריה ראשית: צלם חתונות
  אתר: https://www.amora-studios.com

לגבי כתובת — שים לב, זה חשוב: לסטודיו אין כתובת פיזית שלקוחות מגיעים אליה.
כשגוגל תשאל אם יש כתובת שלקוחות מבקרים בה, ענה שלא, ובחר באפשרות של עסק
שנותן שירות באזור. את אזור השירות הגדר כ"ישראל".
אל תזין כתובת רחוב בשום שלב.

אם גוגל מבקשת שעות פעילות, מספר טלפון, תיאור העסק או תמונות — עצור ושאל
אותי. אל תעתיק מהאתר ואל תנחש. עדיף פרופיל חלקי מפרופיל עם פרט שגוי.

בסוף התהליך גוגל תדרוש אימות בעלות — בגלויה, בטלפון או בווידאו.
דווח לי אילו אפשרויות מוצעות ועצור. אל תתחיל תהליך אימות בעצמך.
```

---

## 2 · שדה `meta_lead_id` באוריגמי

כרגע מזהה הליד נשמר בתוך שדה ההערות (`fld_1494`), בשורה הראשונה, בפורמט
`META_LEAD_ID: <id>`. זה תופס את המידע מרגע זה ואילך, אבל שדה ייעודי עדיף —
אפשר לחפש לפיו ולסנן.

> היכנס למערכת אוריגמי של הסטודיו.
>
> מצא את סוג הישות שבו נשמרים הלידים — זו הישות שמכילה את השדות "שם מלא"
> ו"מספר טלפון" של פניות, והמזהה הפנימי שלה הוא e_89.
>
> הוסף לה שדה טקסט חדש בשם **"מזהה ליד Meta"**. שדה טקסט רגיל, לא חובה, בלי
> ולידציה מיוחדת, אורך מקסימלי 60 תווים.
>
> אחרי היצירה, מצא ודווח לי את **המזהה הפנימי של השדה** — הוא נראה כמו
> `fld_` ואחריו מספר. אני צריך את המספר המדויק. אם המערכת לא מציגה אותו
> בממשק, בדוק בכתובת ה-URL של עמוד עריכת השדה או בקוד המקור של העמוד.
>
> אל תשנה, תמחק או תשנה שם של שום שדה קיים.

**מה לעשות עם התשובה:** תשלח לי את מזהה השדה, ואני אמפה אליו את `{{1.id}}`
בתרחיש Make ואעביר את הנתון מההערות לשדה עצמו.

---

## 3 · Google Search Console לדומיין של אמורה

היום מחובר `regaflash.com` בלבד. כלומר אף אחד לא יודע על מה `amora-studios.com`
מוצג בגוגל. אפס שינוי קוד, אפס פריסה.

> פתח את Google Search Console בכתובת search.google.com/search-console.
>
> הוסף נכס חדש מסוג **Domain** (לא URL prefix) עבור `amora-studios.com`.
>
> גוגל תציג רשומת TXT לאימות. העתק אותה ודווח לי אותה במדויק — את השם ואת
> הערך המלא.
>
> אחר כך פתח לשונית חדשה ב-vercel.com, היכנס לפרויקט של amora, ולך ל-Settings
> → Domains → amora-studios.com → DNS Records. הוסף שם רשומת TXT עם הערך
> שקיבלת מגוגל.
>
> חזור ל-Search Console ולחץ Verify. אם האימות נכשל, המתן חמש דקות ונסה שוב —
> הפצת DNS לוקחת זמן. דווח לי מה קרה.
>
> אל תיגע בשום רשומת DNS קיימת, ובמיוחד לא ברשומות A, CNAME או MX. רק להוסיף.

---

## 4 · Google Business Profile

המנוף המקומי הגדול ביותר שאין לו תחליף בקוד: ה-local pack מופיע מעל התוצאות
האורגניות. נכון להיום הוא לא קיים.

> פתח את business.google.com וצור פרופיל עסקי חדש.
>
> שם העסק: **Amora Studio**
> קטגוריה ראשית: צלם חתונות (Wedding photographer)
> קטגוריות משניות: שירותי צילום וידאו, צלם
> אתר: https://www.amora-studios.com
>
> **חשוב לגבי כתובת:** לסטודיו אין כתובת פיזית שלקוחות מגיעים אליה. בחר באפשרות
> של עסק שנותן שירות באזור (service-area business) והגדר את אזור השירות
> כ"ישראל" או כרשימת ערים שהבעלים יאשר. **אל תזין כתובת רחוב.**
>
> **אל תמציא שום פרט.** אם גוגל מבקשת שעות פעילות, טלפון, תיאור או תמונות
> ואין לך את המידע — עצור ושאל אותי. אל תשלים מהאתר ואל תנחש.
>
> בסוף התהליך גוגל תדרוש אימות (גלויה, טלפון או וידאו). דווח לי איזו שיטה
> מוצעת ועצור שם.

**למה "אל תמציא":** האתר בנוי על הכלל שכל טענה חייבת להיות אמיתית. פרופיל עסקי
עם שעות פעילות שהומצאו הוא בדיוק אותה הפרה.

---

## 5 · מאסטר לוגו

הגדלת הלוגו נבדקה ואומתה (RMSE 3.49, האותיות זהות) אבל לא הצלחתי להוריד את
הקובץ — הפרוקסי של הסביבה שלי חוסם את ה-CDN.

> הורד את הקובץ מהכתובת הבאה ושמור אותו בשם `logo-4k.png`:
>
> https://d8j0ntlcm91z4.cloudfront.net/user_3GptPBn0PtejbdXxIAQaNLNkJaD/hf_20260804_231943_6c2236f1-4a9d-4f11-8e6f-36d7214ed09d.png
>
> פתח אותו והשווה אותו ללוגו הנוכחי באתר בכתובת
> https://www.amora-studios.com/assets/img/logo.jpg — האותיות של המילים
> AMORA ו-STUDIO חייבות להיות זהות בצורתן. דווח לי אם משהו נראה שונה.

**מה לעשות עם הקובץ:** לשים אותו בריפו ולהגיד לי — אקטין ל-512, אחליף את
`logo.jpg` ואריץ מחדש את יצירת האייקונים, כך שהם יפסיקו להיות הגדלה של 150px.
