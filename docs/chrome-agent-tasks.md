# פקודות ל-Claude in Chrome

משימות שדורשות ממשק דפדפן שאין לי גישה אליו. כל בלוק להעתקה כמו שהוא.

**כלל בטיחות:** Claude in Chrome פועל בדפדפן שלך עם ההרשאות שלך. אל תיתן לו
לאשר תשלום, למחוק, ליצור חשבון או לשנות תקציב. אם משימה דורשת אחד מאלה והיא
לא נכתבה כך במפורש — זו סטייה, ויש לעצור.

**לכל סעיף כאן יש נמען, והוא מסומן בכותרת:**

- 🧑 **לבעלים** — דורש סוד, אישור, או עריכה במערכת חיה. סוכן דפדפן צריך
  לסרב לאלה, וסירוב כזה הוא התנהגות נכונה ולא תקלה.
- 🤖 **לסוכן** — קריאה, מלאי, או פעולה תחומה שנכתבה במפורש.

הסימון הזה נוסף אחרי שסוכן קרא סעיף שמיועד לבעלים, זיהה נכון שהוא מתבקש
לשלוף סוד חי ולהדביק אותו לאוטומציה עסקית, וסירב. הוא צדק. הקובץ הוא שלא
אמר לו למי כל חלק מיועד.

---

## מצב — 5.8.2026

| משימה | מצב |
| --- | --- |
| ביטול האסימון שנחשף | **בוצע** ✓ |
| אסימון CAPI חדש ב-Supabase | **בוצע ואומת מול ה-API** ✓ |
| מבנה ה-payload | **אומת** — ראו `docs/meta-capi-flow.md` ✓ |
| שדה `meta_lead_id` באוריגמי | **בוצע — `fld_1519`, ממופה בתרחיש** ✓ |
| רשימת 14 הסטטוסים | **בוצע — נקראה מה-API, לא צריך דפדפן** ✓ |
| מודולי ManyChat שנמחקו | **שוחזרו 5.8 13:03** ✓ |
| Search Console לדומיין אמורה | **היה מאומת מ-4.8. הסטטוס כאן היה שגוי** ✓ |
| התראות לידים | **חיות, דומיין מאומת, שתי תיבות** ✓ |
| כותרות SPF/DKIM/DMARC | **נקראו — SPF PASS, DKIM PASS, DMARC אין רשומה** ✓ |
| אירועי CAPI | **ארבעה נשלחו והתקבלו 5.8** ✓ |
| נחיתת האירועים ב-Meta | **אומת בממשק 5.8 — ארבעה, אפס שגיאות** ✓ |
| אופטימיזציה ל-Conversion Leads | **פתוח — "בשימוש על ידי" ריק** |
| טריגר אמיתי מאוריגמי | פתוח — **אבל הקמפיין לא כבוי.** תרחיש 6821619 רץ כל יום, 6.8–13.8 |
| אימות דומיין ב-Resend | **בוצע 5.8 18:14** ✓ |
| חיבור CAPI ב-Make 3756300 | **בוצע 5.8 15:24** ✓ |
| פרופיל עסקי בגוגל | **נדחה בהחלטת הבעלים** |

## 🧑+🤖 הפקודה הפעילה (14.8) — הפעלה, ואימות ראשון של האתר החי

**למה עכשיו:** ב-13.8 נדחפו תשעה עמודים, CSP שעבר לאכיפה, טופס דו-שלבי,
נגזרות AVIF ותג Analytics. **אף אחד מהם לא נראה בפרודקשן.** הפרוקסי של
סביבת העבודה חוסם את `amora-studios.com` ב-403, אז יש כאן פער אמיתי בין
"הבדיקות ירוקות" לבין "האתר עובד".

המשימה הראשונה היא הסעיף היחיד ב-P0: בלי מדידה אי אפשר להבחין בין "אף אחד
לא מגיע" ל"כולם נוטשים", והתשובות לשתיהן הפוכות.

```
אתה עוזר לי בתפעול Amora Studio.

חוקים לכל המשימות כאן:
· אל תאשר תשלום, אל תשדרג תוכנית ואל תוסיף אמצעי תשלום. אם מסך
  מבקש אחד מאלה — עצור ותאר לי בדיוק מה הוא מבקש. זה לא כישלון,
  זו התנהגות נכונה.
· אל תמחק שום דבר ואל תשנה הגדרה שלא נכתבה כאן במפורש.
· אם מסך נראה אחרת ממה שתיארתי — עצור, צלם ותאר. אל תנחש.

─────────────────────────────
משימה 1 — להפעיל Web Analytics   (הכי חשובה)
─────────────────────────────

היכנס ל-https://vercel.com/dashboard ופתח את הפרויקט שמשרת את
amora-studios.com. אם יש כמה פרויקטים — תגיד לי את השמות ואל
תנחש איזה.

עבור ללשונית Analytics.

אתה מחפש את Web Analytics — לא Speed Insights. אלה שני מוצרים
נפרדים ואני צריך רק את הראשון.

לחץ Enable.

עצור ודווח לי, בלי לאשר, אם המסך מבקש:
   · לשדרג ל-Pro או לכל תוכנית בתשלום
   · להוסיף כרטיס אשראי
   · לאשר חיוב או מכסת אירועים

אם ההפעלה עברה בלי אף אחד מאלה — תגיד לי, ואז המשך למשימה 2.

אין צורך לפרוס מחדש. התג כבר יושב בעמודים.

─────────────────────────────
משימה 1ב — מסלול חלופי, מהיר יותר (אם נוח לך טרמינל)
─────────────────────────────

אם יש לך גישה לטרמינל, שלוש פקודות עושות את אותו הדבר בלי
לחפש בממשק:

   npx vercel login
   npx vercel link
   npx vercel project web-analytics --format json

הפקודה האחרונה מחזירה אישור בפורמט JSON, כלומר תשובה חד-משמעית
במקום לנחש אם הלחיצה תפסה. אם היא מחזירה שגיאה — העתק אותה לי
כמו שהיא.

─────────────────────────────
משימה 2 — האתר החי, תשעה עמודים
─────────────────────────────

פתח כל אחת מהכתובות האלה ותגיד לי לגבי כל אחת: נטענה (כן/לא),
ואם לא — מה הסטטוס.

   https://www.amora-studios.com/
   https://www.amora-studios.com/cost.html
   https://www.amora-studios.com/magnets.html
   https://www.amora-studios.com/delivery.html
   https://www.amora-studios.com/photographers.html
   https://www.amora-studios.com/low-light.html
   https://www.amora-studios.com/save-the-date.html
   https://www.amora-studios.com/corporate.html
   https://www.amora-studios.com/camera-3d.html

ואז, בעמוד הבית בלבד, ארבע בדיקות:

א. גלול לתחתית. בפוטר צריך להיות טור בשם "עמודי תוכן" עם שמונה
   קישורים (מה משפיע על המחיר, כמה זמן עד התמונות, צלם אחד או
   שלושה, אולם חשוך, מגנטים עם וידאו, Save the Date, אירועי
   חברה, המצלמה שלנו). תגיד לי אם הוא שם וכמה קישורים בו.

ב. פתח את קונסולת הדפדפן (F12 ← Console) ורענן. אני מחפש
   שגיאות אדומות, ובמיוחד כל שורה שמכילה את המילה
   "Content Security Policy". העתק לי כל שורה כזו במלואה.
   אם הקונסולה נקייה — תגיד "נקייה".

ג. גלול לטופס "בדיקת זמינות" בתחתית. הוא צריך לבקש רק שם
   וטלפון, ולהראות "שלב 1 מתוך 2". מלא שם כלשהו ומספר טלפון
   אמיתי שלך, לחץ על הכפתור, ותגיד לי אם הוא עבר לשלב 2 עם
   שדות של תאריך וסוג אירוע.
   אל תשלח את הטופס בסוף — אני לא רוצה ליד מזויף במערכת.
   פשוט תגיד לי מה ראית ואז סגור את הלשונית.

ד. בתפריט העליון יש כפתורי שפה (עברית · العربية · English ·
   Русский). לחץ English. תגיד לי אם העמוד עבר לאנגלית, כמה
   זמן זה לקח, והאם משהו נשאר בעברית.

ואז פתח את https://www.amora-studios.com/magnets.html **בטלפון**
(או בתצוגת מובייל, רוחב 390px) וגלול לתרשים שלושת השלבים.
תגיד לי אם הכיתובים מתחת לאיורים קריאים. זה החלק שתיקנתי היום
ולא ראיתי בפועל.

─────────────────────────────
משימה 3 — Search Console
─────────────────────────────

פתח https://search.google.com/search-console ובחר את הנכס
sc-domain:amora-studios.com

לך ל-Sitemaps. אם הוא כבר מופיע — תגיד לי מה הסטטוס ומתי נקרא
לאחרונה. אם לא, הגש אותו עם **הכתובת המלאה**:

   https://www.amora-studios.com/sitemap.xml

חשוב: נכס מסוג Domain דוחה כתובת יחסית עם השגיאה
"Invalid sitemap address". חייבים את ה-https המלא.

ואז, ב-URL Inspection למעלה, הדבק כל אחת מהשש ולחץ
"Request Indexing":

   https://www.amora-studios.com/magnets.html
   https://www.amora-studios.com/delivery.html
   https://www.amora-studios.com/photographers.html
   https://www.amora-studios.com/low-light.html
   https://www.amora-studios.com/save-the-date.html
   https://www.amora-studios.com/corporate.html

אם גוגל אומר שהגעת למכסה יומית — תגיד לי כמה הספקת ואיזו
נשארה, ונמשיך מחר.
```

---

## 🤖 שתי שאלות קריאה בלבד — לידים שנפלו, ו-HubSpot

שתיהן קריאה בלבד. הראשונה נוגעת ללידים אמיתיים של אנשים אמיתיים; השנייה
חוסמת החלטה על כל שדרוג CRM עתידי.

```
אתה עוזר לי בתפעול Amora Studio. שתי המשימות האלה הן קריאה בלבד.
אל תיצור, אל תערוך, אל תמחק ואל תשנה שום סטטוס. אם אתה לא מוצא
משהו — תגיד לי שלא מצאת. אל תשלים פערים בניחוש.

─────────────────────────────
משימה 1 — שלושה לידים שאולי נפלו   (הדחופה)
─────────────────────────────

ב-10.8.2026 שלוש הרצות של תרחיש Make נכשלו עם השגיאה
"The string supplied did not seem to be a phone number", ואחת מהן
גם עם שגיאת אוריגמי "fld_1509/טלפון - פורמט מספר טלפון לא חוקי".
שעות הכישלון: בערך 10:25, 10:31 ו-17:05.

פתח https://eu1.make.com ולך לתרחיש
"אמורה — לידים פייסבוק ← HubSpot + אוריגמי + WhatsApp" (מספר 6821619).

בלשונית History מצא את שלוש ההרצות האדומות של 10.8.
לכל אחת, פתח את פרטי ההרצה והעתק לי:
   · השעה המדויקת
   · השם והטלפון של הליד כפי שהגיעו מפייסבוק

ואז פתח את אוריגמי וחפש כל אחד משלושת הטלפונים.
תגיד לי לגבי כל אחד: קיים באוריגמי (כן/לא).

זה החלק שחשוב: ליד שנכשל ולא קיים באוריגמי הוא בן אדם שהשאיר
פרטים ואף אחד לא חזר אליו. אם יש כזה — תגיד לי מיד.

─────────────────────────────
משימה 2 — מה HubSpot מקבל
─────────────────────────────

באותו תרחיש ב-Make, ההוא עם HubSpot בשם.

תגיד לי:
   · אילו מודולים יש בו, לפי הסדר
   · מה בדיוק המודול של HubSpot עושה — יוצר איש קשר? עסקה?
     מעדכן קיים?
   · אילו שדות הוא שולח

ואז פתח את HubSpot ותגיד לי כמה אנשי קשר יש שם ומתי נוצר
האחרון.

השאלה שאני מנסה לענות עליה: האם HubSpot הוא מקור אמת שני לצד
אוריגמי, או שהוא רק מראה. זה קובע לאן מחברים את טופס האתר,
ואני לא רוצה לבנות מול המערכת הלא נכונה.
```

---

### מה נלמד בדרך

הפעם הראשונה שאסימון נשמר, הוא היה למעשה האסימון הישן שבוטל אחריו. זה נתפס רק
משום שנבדק בפועל מול `graph.facebook.com` ולא הונח שהוא תקין.
**אסימון מאמתים לפני ששומרים, לא אחרי שבונים עליו.**

בנוסף, מסלול שכתבתי דרש רישום כמפתח פייסבוק — צעד מיותר לחלוטין, שהסוכן צדק
כשעצר ושאל עליו. יצירת אסימון CAPI נעשית ב-Events Manager, שהוא כלי עסקי.

**והשלישי, שמחק שתי פקודות מהקובץ הזה:** שלחתי אותך לקרוא באוריגמי דברים
שה-API של Make יודע להחזיר בעצמו. `rpc://origami@1/instanceGroup` ו-
`rpc://origami@1/mappableFields` מחזירים את כל השדות של `e_89` — כולל
`fld_1519`, כולל 14 ערכי הסטטוס, כולל האפשרויות של `fld_1357`. **לפני ששולחים
בן אדם למסך, בודקים אם יש RPC.**

---

## ✓ הושלם — מזהה ה-Flow של ManyChat

**מה קרה:** ב-5.8 בשעה 10:02 עדכנתי את תרחיש `6821619` בעזרת בלוprint שמשכתי
יממה קודם. `scenarios_update` מחליף בלוprint שלם ולא ממזג, ולכן **מודולי
ManyChat שהוספת ביום שלפני נמחקו.** זו טעות שלי.

הכול חוץ ממזהה ה-Flow שוחזר מה-API (חיבור `3734857`, חשבון `fb3819002`, צורת
המודול, והתבנית המלאה מתרחיש `3838465`). מזהה ה-Flow הוא שדה טקסט חופשי ב-Make
ואין RPC שמונה Flows — ולכן היה צריך עיניים במסך.

**התשובה, 5.8.2026:** `content20260804141448_064986` — "Amora - New Lead
WhatsApp", מסומן LIVE, מועמד יחיד בחשבון. התרחיש שוחזר ב-13:03. הפירוט המלא
ב-`docs/meta-capi-flow.md`.

**שני דברים שהסוכן עשה נכון וכדאי לחזור עליהם:** הוא דיווח על נוסח ההודעה
כ"קירוב, לא ורבatim" במקום להעביר טקסט מטושטש כעובדה, והוא עצר לפני פתיחת
עורך מודול — קריאה, אבל קליק אחד מעבר להיקף שסוכם. שתי ההתנהגויות האלה הן
מה שהופך דיווח מסוכן דפדפן לשמיש.

<details>
<summary>הפקודה כפי שנשלחה (לשימוש חוזר)</summary>

```
אתה עוזר לי בתפעול העסק שלי — Amora Studio, סטודיו לצילום ווידאו לחתונות
בישראל.

חוקים:
- אל תמחק, תשנה, תפרסם או תשכפל שום Flow, אוטומציה, תרחיש או גרסה. קריאה
  בלבד.
- אל תאשר תשלום או שדרוג חבילה. אם מסך כזה מופיע — עצור ושאל.
- אל תמציא נתונים. אם אתה לא בטוח מה מולך — עצור ותאר לי מה אתה רואה.

─────────────────────────────
דרך א — ManyChat (העדיפה)
─────────────────────────────

פתח את https://app.manychat.com/fb3819002/cms
זו רשימת ה-Flows של החשבון.

חפש Flow שנועד ללידים חדשים מקמפיין פייסבוק של אמורה — שם בסגנון "אמורה",
"ליד חדש", "פנייה חדשה" או "ברוכים הבאים". אם יש כמה מועמדים, אל תבחר —
דווח לי את כולם.

לכל מועמד:
  1. פתח אותו
  2. העתק את כתובת ה-URL המלאה מהדפדפן
  3. דווח לי את שם ה-Flow ואת שתי-שלוש השורות הראשונות של ההודעה שהוא שולח

מה שאני צריך מה-URL הוא המקטע שמתחיל ב-content, למשל:
content20251121100944_073243

בדוק גם, ודווח לי: האם ה-Flow הזה מסומן כ-Published או כ-Draft?
(זה קריטי — ריצה אחת ב-4.8 נכשלה בדיוק על "Automation must be published".)

─────────────────────────────
דרך ב — היסטוריית גרסאות ב-Make (רק אם דרך א לא נתנה תשובה ברורה)
─────────────────────────────

פתח את https://eu1.make.com ואת התרחיש "אמורה — טפסי לידים פייסבוק ← אוריגמי"
(מזהה 6821619).

חפש היסטוריית גרסאות — בדרך כלל בתפריט שלוש הנקודות בפינה התחתונה, תחת
"Previous versions". מצא גרסה מ-04/08/2026 בין 16:00 ל-22:00; בגרסה הנכונה
התרחיש נקרא "אמורה — לידים פייסבוק ← אוריגמי + WhatsApp".

פתח אותה לצפייה בלבד — אל תלחץ Restore.
דווח לי:
  1. כמה מודולים של ManyChat יש, ומה השם של כל אחד
  2. מה כתוב בשדה Flow ID
  3. באיזה ענף של הראוטר הם יושבים, ואחרי איזה מודול

─────────────────────────────

עצור כאן ודווח. אל תשחזר גרסה ואל תפעיל כלום.
```

**למה אסור היה ללחוץ Restore:** גרסה מ-4.8 לא מכילה את התיקונים שנכנסו מאז
(`fld_1519`, ו-`fld_1357` שתוקן לערך חוקי). שחזור היה מוחק אותם. במקום זה
נכתב בלוprint אחד שמכיל את הכול — עם משיכה טרייה שנייה אחת לפני הכתיבה.

</details>

---

## ✓ הושלם — חיבור ה-CAPI בתרחיש 3756300

> **סעיף זה אינו לסוכן דפדפן.** הוא דורש שליפת סוד חי והדבקתו לאוטומציה
> עסקית פעילה. סוכן שיסרב לבצע אותו — צודק.

נקודת הקצה בנויה, פרוסה ונבדקה (`supabase/functions/meta-capi`, חמש בדיקות מול
המערכת החיה — ראו `docs/meta-capi-flow.md`). מה שנשאר הוא שני מודולים בתרחיש
**`3756300` — "עדכון סטטוס לקוח במאני צאט"**, שהוא נקודת החיבור הנכונה משום
שהוא מקבל **כל** שינוי סטטוס, ולא סטטוס אחד.

### למה לא עשיתי את זה בעצמי — הנימוק הראשון היה שגוי, השני לא

**הנימוק שכתבתי כאן קודם היה מוטעה ותוקן.** טענתי ש-`scenarios_update` מחייב
אותי להעתיק ידנית רשימת enum של כ-250 קודי מדינה מתוך ה-`metadata` של מודול
ה-`phonenumber`, ושזה מסוכן מדי. **בדקתי את ההנחה במקום להמשיך להסתמך עליה:**
יצרתי תרחיש זמני עם אותו מודול בלי `metadata.expect` בכלל — Make החזיר
`isinvalid: false`, שמר את הבלוprint כפי שנשלח, והתרחיש תקין. מחקתי אותו.

**המסקנה:** `metadata.expect` ו-`samples` הם מטא-דאטה של העורך, לא קונפיגורציית
הרצה. Make מייצר אותם מחדש. מה שקריטי להרצה הוא `module`, `mapper`,
`parameters`, `filter`, `routes` ו-`onerror` — כולם קצרים. הסיכון שתיארתי
פשוט לא היה קיים בגודל שייחסתי לו.

**מה כן עצר אותי בפועל:** מסווג ההרשאות של הסביבה חסם את קריאת
`scenarios_update` על התרחיש החי. זו חסימה לגיטימית — כתיבה מחליפה על אוטומציה
עסקית פעילה היא בדיוק הפעולה שהרסה כאן מודול פעם אחת — ולא ניסיתי לעקוף אותה.

**שתי דרכים קדימה:** לאשר את ההרשאה ואני כותב את הבלוprint בקריאה אחת (הוא
מוכן, עם משיכה טרייה רגע לפני), או להדביק את שני המודולים ידנית לפי ה-JSON
שלמטה. שתיהן מגיעות לאותה תוצאה.

### הסוד — לא נמצא בריפו, בכוונה

הריצו את זה ב-Supabase ← SQL Editor והעתיקו את הערך:

```sql
select value from private.settings where key = 'meta_capi_hook_secret';
```

### שני המודולים

ב-Make אפשר להדביק מודול ישירות לקנבס (Ctrl+V). פתחו את התרחיש, הוסיפו
**נתיב שלישי** לראוטר (זה שיושב אחרי מודול 2), והדביקו לתוכו את השניים לפי
הסדר. הנתיב השלישי **בלי פילטר** — נקודת הקצה היא זו שמחליטה מה נשלח.

מודול א — חיפוש הליד באוריגמי כדי לקרוא את `fld_1519`:

```json
{
  "id": 20,
  "module": "origami:search",
  "version": 1,
  "parameters": { "__IMTCONN__": 3523393 },
  "mapper": {
    "limit": "1",
    "query": [[{ "a": "fld_1509", "b": "0{{8.phone}}", "o": "=" }]],
    "orderdir": "desc",
    "entityname": "e_89",
    "searchMethod": "andxor",
    "with_archive": 0
  },
  "onerror": [{ "id": 22, "module": "builtin:Ignore", "version": 1 }],
  "metadata": { "designer": { "x": 900, "y": 600 } }
}
```

מודול ב — שליחה לנקודת הקצה. **החליפו את `PASTE_SECRET_HERE`** בערך מה-SQL:

```json
{
  "id": 21,
  "module": "http:ActionSendData",
  "version": 3,
  "parameters": { "handleErrors": false, "useNewZLibDeCompress": true },
  "mapper": {
    "url": "https://dkejuaildigikufrdiru.supabase.co/functions/v1/meta-capi",
    "method": "post",
    "headers": [{ "name": "x-amora-capi-secret", "value": "PASTE_SECRET_HERE" }],
    "bodyType": "raw",
    "contentType": "application/json",
    "data": "{\"lead_id\":\"{{20.fld_1519}}\",\"status\":\"{{1.`סטטוס ליד`}}\"}",
    "parseResponse": true,
    "serializeUrl": false,
    "shareCookies": false,
    "rejectUnauthorized": true,
    "followRedirect": true,
    "useQuerystring": false,
    "gzip": true,
    "useMtls": false
  },
  "onerror": [{ "id": 23, "module": "builtin:Ignore", "version": 1 }],
  "metadata": { "designer": { "x": 1200, "y": 600 } }
}
```

**שלוש הגנות, כי זו אוטומציה חיה:** `handleErrors: false` אומר שתשובה שאינה
2xx נרשמת כקוד סטטוס ולא ככשל; `builtin:Ignore` על שני המודולים אומר שגם כשל
אמיתי לא מפיל את הריצה; והנתיב השלישי נפרד משני הקיימים, כך ששני המודולים של
ManyChat אינם מושפעים בשום מקרה.

**מה יקרה בפועל:** רוב שינויי הסטטוס יקבלו `200 {"skipped":true}` — זה תקין
ומכוון. חמישה סטטוסים בלבד מייצרים אירוע.

### ואז — ורק אז

תגידו לי שזה מחובר, ואני מוסיף את פסקת ה-CAPI ל-`privacy.html`. הנוסח כתוב
ומחכה ב-`docs/meta-capi-flow.md`. הוא **לא** נכנס לפני שאירוע אמיתי נשלח:
עמוד שמצהיר על משהו שאינו קורה שגוי בדיוק כמו עמוד ששותק על משהו שקורה. הפסקה
נכתבה היום, נבדקה, ונמשכה בחזרה מהאתר מהסיבה הזו בדיוק.

---

## 🤖 ~~Search Console~~ — הושלם, והתברר שכבר היה עשוי

**הנכס `amora-studios.com` אומת ב-4.8.2026 על ידי הבעלים** — Domain property
(`sc-domain:`), לא URL-prefix, עם ✓ ירוק ב-Ownership verification. שתי רשומות
`google-site-verification` כבר יושבות על `@` ב-Hostinger. חלק ב מעולם לא היה
נחוץ.

**אומת פעמיים, משני מקורות בלתי תלויים:** בדיקת דפדפן בקריאה בלבד, ומייל של
גוגל עצמה בתיבה של הסטודיו — *"Google systems show that you recently verified
this Domain property"*, 4.8 בשעה 17:56.

### שלושה דברים שהמלאי הזה גילה, שאף אחד מהם לא היה המשימה

1. **ה-DNS יושב ב-Hostinger, לא ב-Vercel.** נעילת שרתי שמות
   `ns1/ns2.dns-parking.com`, ו-`www` הוא CNAME ל-vercel-dns. שתי הטענות
   נכונות במקביל: Vercel מארח, Hostinger מנהל DNS. `CLAUDE.md` תוקן.
2. **`MX` על `@` מצביע לגוגל** — לדומיין יש דואר עובד. משם נמצא
   `support@amora-studios.com`, שחוסם את `LEAD_ALERT_TO` כבר חודשים בזמן
   שהוא קיים.
3. **הנכס כבר מייצר ערך.** מייל מ-5.8 דיווח על שני ליקויי structured data
   בתמונות. אחד תוקן (`creditText`), אחד **נדחה במכוון**
   (`acquireLicensePage` — ראו `tools/gen-image-schema.py`).

### הלקח, והוא לא על גוגל

הסטטוס בקובץ הזה אמר "פתוח" בזמן שהמשימה הושלמה יום קודם. הפקודה שכתבתי
פתחה ב"הוסף נכס חדש" — פעולה שגויה שהייתה יוצרת כפילות. **סוכן שהתחיל
במלאי במקום בפעולה מנע את זה**, וזו הסיבה שכל פקודה כאן מתחילה מעכשיו
בבדיקת מה קיים.

זו אותה תבנית שחוזרת בפרויקט: **מצב שנרשם אינו מצב שנבדק.**

---

## 🤖+🧑 אימות הדומיין ב-Resend — חלק א הושלם, חלק ב ממתין

**חלק א בוצע 5.8.2026.** הדומיין `amora-studios.com` נוסף לחשבון **amora**
ב-Resend, אזור Ireland (eu-west-1), Enable Sending דלוק ו-**Enable Receiving
כבוי בכוונה**. שום רשומת DNS לא נגעה עדיין.

**שני ממצאים שנשמרו ב-`docs/lead-alerts.md`:**

1. **מלכודת החשבון.** התוכנית החינמית מרשה דומיין אחד. בחשבון regaflash כבר
   מאומת `regaflash.com`, ולכן `Add domain` שם החזיר מסך שדרוג ל-Pro ב-$20
   לחודש. הסוכן **לא שדרג ולא אישר תשלום** — נכון. הדומיין שייך לחשבון amora,
   שבו אין אף דומיין ואין חסימה. מסך תשלום כאן = החשבון שגוי, לא התוכנית.
2. **מלכודת ה-MX התממשה.** ל-Resend יש מתג *Enable Receiving* שדורש `MX` על
   **`@`**. זו בדיוק הרשומה שהייתה דורסת את ה-MX של גוגל ומורידה את הדואר.
   היא כבויה, ואינה נדרשת לא לאימות ולא לשליחה.

### מה מאושר להוספה — 1–3 בלבד

| # | סוג | שם | תפקיד |
| --- | --- | --- | --- |
| 1 | TXT | `resend._domainkey` | מפתח DKIM |
| 2 | MX | `send` (priority 10) | משוב החזרות |
| 3 | TXT | `send` | SPF לתת-הדומיין השולח |

2 ו-3 יושבות על **`send`** ולא על `@`, ולכן ה-SPF הראשי וה-MX של גוגל
אינם מושפעים.

**`_dmarc` (#4) — לא מוסיפים.** `_dmarc` חייבת להיות רשומה **יחידה**; דומיין
עם שתיים נחשב כחסר מדיניות לגמרי, כלומר גרוע יותר מלא לגעת. לא ידוע אם כבר
קיימת אחת — המלאי תפס סוגים ושמות בלבד. בנוסף DMARC חלה על **כל** דואר
הדומיין כולל ה-Gmail של הסטודיו, ו-`p=none;` בלי `rua=` לא מפיק דוחות ולא
אוכף דבר. אם ירצו בה בעתיד, הצעד הראשון הוא **לקרוא** את `_dmarc`.

```
חלק ב — הוספה ב-Hostinger. רשומות 1-3 בלבד.

חוקים: רק להוסיף. אסור לערוך, למחוק או להחליף שום רשומה קיימת.
אם המערכת מציעה replace או overwrite — עצור ושאל.
אל תוסיף את רשומת ה-_dmarc.
אל תדליק Enable Receiving ב-Resend.

פתח https://hpanel.hostinger.com/domains/dns ובחר amora-studios.com.

הוסף שלוש רשומות חדשות:
  1. TXT   שם: resend._domainkey   ערך: העתק מ-Resend בכפתור Copy
  2. MX    שם: send   ערך: feedback-smtp.eu-west-1.amazonses.com   priority 10
  3. TXT   שם: send   ערך: v=spf1 include:amazonses.com ~all

את ערך ה-DKIM העתק בלחיצה על Copy ב-Resend והדבק. אל תקליד אותו מחדש
ואל תרכיב אותו מהמסך — תו אחד שגוי מפיל את האימות בלי הסבר.

אחרי כל שמירה, קרא את השורה כפי שהיא מוצגת. אם השם הפך ל-
resend._domainkey.amora-studios.com.amora-studios.com — הפאנל הוסיף את
הדומיין לבד. תקן ודווח.

ודא שרשומת ה-MX החדשה היא על send ולא על @. אם היא נשמרה על @ — מחק אותה
מיד ודווח לי, זו הרשומה שמפילה את הדואר.

חזור ל-Resend ולחץ Verify. אם נכשל — חכה 10 דקות ונסה שוב.
דווח את הסטטוס של כל שלוש הרשומות. עצור כאן.
```

**מה אעשה כשתדווח שאומת:** אגדיר `LEAD_ALERT_FROM` לכתובת על הדומיין, אריץ
ליד בדיקה אמיתי, אאמת `200 sent:true` ואמחק את השורה — בדיוק כמו הבדיקה
שעברה היום.

---

## 🤖 הפקודה הפעילה — שתי בדיקות קריאה שסוגרות פערים אמיתיים

שתיהן קריאה בלבד. הראשונה סוגרת את הפער היחיד שנשאר פתוח בצינור ההתראות;
השנייה מכינה קו בסיס ל-CAPI לפני האירוע האמיתי הראשון.

```
אתה עוזר לי בתפעול Amora Studio.

חוקים: קריאה בלבד בשתי המשימות. אל תמחק, אל תשיב, אל תסמן כספאם,
אל תשנה שום הגדרה ואל תאשר תשלום. אם משהו נראה אחרת ממה שתיארתי —
עצור ותאר לי מה אתה רואה.

─────────────────────────────
משימה 1 — כותרות האימות של המייל  (החשובה)
─────────────────────────────

פתח את תיבת support@amora-studios.com בג'ימייל.
חפש: "פנייה חדשה"

פתח את ההודעה האחרונה (מ-Amora Studio <leads@amora-studios.com>).

בתפריט שלוש הנקודות של ההודעה בחר "הצג את המקור"
(Show original). זה נפתח בלשונית או בחלון חדש.

בראש העמוד יש טבלה קטנה. דווח לי בדיוק מה כתוב בשלוש השורות:
   SPF:    ___
   DKIM:   ___
   DMARC:  ___

אם אחת מהן אומרת FAIL, SOFTFAIL, NEUTRAL או NONE — העתק את השורה
המלאה כולל הדומיין שמופיע בסוגריים.

אם החלון החדש לא נפתח או לא נגיש לך — תגיד לי את זה במפורש
ואל תנחש מה היה כתוב שם. זה קרה כבר פעם אחת וזה בסדר גמור.

─────────────────────────────
משימה 2 — קו בסיס ב-Events Manager
─────────────────────────────

פתח https://business.facebook.com/events_manager2
ובחר את מערך הנתונים 2851332215058775.

עבור ללשונית שבה רואים אירועים שהתקבלו לאחרונה
("Overview" או "Test events" / "Data sources").

דווח לי:
  1. אילו סוגי אירועים מופיעים ב-7 הימים האחרונים, ובאיזה נפח
  2. האם מופיע אירוע בשם LeadConverted, LeadContacted,
     LeadQualified או LeadDisqualified — ואם כן, כמה
  3. האם יש התראות או שגיאות מוצגות על מערך הנתונים
  4. אם יש מדד "Event Match Quality" או "אחוז התאמה" —
     דווח את הערך שלו

אני מצפה שעדיין לא יופיעו אירועי Lead* — הצינור חובר היום
ואף שינוי סטטוס אמיתי עוד לא קרה. אם כן מופיעים — זה מעניין
ואני רוצה לדעת.

עצור כאן ודווח על שתי המשימות.
```

**למה משימה 1 אינה פורמליות:** "הגיע ל-Inbox" הוא ראיה חלשה. שרת מקבל יכול
לקבל היום ולסנן מחר. `SPF=PASS` ו-`DKIM=PASS` הן הראיה החזקה — ובמיוחד כאן,
כי לדומיין **אין SPF על השורש בכלל**. אם SPF יחזור `NONE` או `FAIL`, זו
הנקודה שבה נדע שצריך להחליט על SPF לשורש, בעיניים פקוחות ולא כניחוש.

**למה משימה 2 עכשיו ולא אחרי האירוע הראשון:** קו בסיס לפני הוא מה שהופך את
האירוע הראשון לניתן לזיהוי. בלי לדעת מה היה שם קודם, "רואים אירוע" לא מוכיח
שהוא שלנו.

---

## ✓ הושלם — אימות שארבעת אירועי ה-CAPI נחתו

**למה זה נחוץ:** האסימון שלנו הוא לכתיבה בלבד — חמש נקודות קצה של קריאה
החזירו `(#100) Missing Permission`. צד השליחה מאומת מלא (`events_received: 1`,
`messages: []` בארבעת האירועים), אבל **צד הקבלה ניתן לראייה רק בממשק.**

**שתי מלכודות שהפקודה עוקפת:** האירועים נשלחו **בלי** `test_event_code`, ולכן
הם אירועים אמיתיים ו**אינם בלשונית "בדיקת אירועים"**. וטווח התאריכים שברירת
המחדל של Meta נותנת **הסתיים ב-4.8** — כלומר לא כולל את היום שבו הם נשלחו.

```
אתה עוזר לי בתפעול Amora Studio.

חוקים: קריאה בלבד. אל תשנה הגדרות, אל תשלים אשפי חיבור, אל תיצור
מערך נתונים, אל תאשר תשלום. שינוי פילטר תצוגה מותר — זה מה שהמשימה דורשת.
אם משהו נראה אחרת ממה שתיארתי — עצור ותאר לי מה אתה רואה.

─────────────────────────────
שלב 1 — לפתוח את המערך הנכון
─────────────────────────────

פתח:
https://business.facebook.com/events_manager2/list/dataset/2851332215058775/overview?business_id=501463492690274

ודא שבראש העמוד כתוב "Regaflash Event Data" ומזהה 2851332215058775.
אם לא — עצור ודווח מה כן מוצג.

─────────────────────────────
שלב 2 — לתקן את טווח התאריכים  (קריטי)
─────────────────────────────

בורר התאריכים בפינה מציג ברירת מחדל שמסתיימת ב-4 באוגוסט.
האירועים נשלחו ב-5 באוגוסט בערב, ולכן בטווח הזה הם לא יופיעו.

שנה את הטווח ל-"היום" (5 באוגוסט 2026). אם אין "היום",
בחר טווח מותאם שמתחיל ומסתיים ב-5.8.2026.

─────────────────────────────
שלב 3 — לחפש את ארבעת האירועים
─────────────────────────────

אני מחפש ארבעה אירועים שנשלחו בערך בין 19:45 ל-20:15:

  LeadContacted
  LeadConverted
  LeadQualified
  LeadDisqualified

דווח לי לכל אחד: האם הוא מופיע, וכמה פעמים.
אם אף אחד לא מופיע — אמור זאת במפורש.

דווח גם:
  - מה כתוב בעמודה "Event Match Quality" / "איכות התאמת אירועים",
    אם יש לה ערך
  - האם מופיעה שגיאה או אזהרה כלשהי על האירועים האלה
  - מה מקור האירוע שמוצג לידם (Server / API של המרות / אחר)

─────────────────────────────
שלב 4 — רק אם שלב 3 החזיר אפס
─────────────────────────────

זה ממצא חשוב ולא כישלון. Meta אישרה קבלה, ולכן אם הם לא כאן
הם נחתו במקום אחר.

במקרה הזה בלבד: פתח את המערך השני בחשבון —
"רגע פלאש - רוקט דיגיטל", מזהה 688868383468576 — עם אותו טווח
תאריכים, ובדוק אם ארבעת האירועים מופיעים שם.

דווח מה מצאת. אל תשנה כלום בשני המערכים.

─────────────────────────────

עצור כאן ודווח.
```

**מה אעשה עם התשובה:** אם הם שם — השרשרת מאומתת מקצה לקצה ואפשר לסמוך עליה
כשהקמפיין יידלק. אם הם לא — `events_received: 1` מול מערך ריק פירושו שהאירועים
נחתו במקום אחר, וזה פער אמיתי שצריך למצוא ולא להניח שנסגר.

---

## 🧑 נדחה — פרופיל עסקי בגוגל

הבעלים החליט לדחות. התהליך נעצר במסך "הזן כתובת דואר לאימות", שדורש כתובת
פיזית אמיתית לקבלת גלויה.

**מה זה עולה:** ה-local pack מופיע מעל התוצאות האורגניות, וזה המנוף המקומי
היחיד ששום קוד בריפו לא יכול לייצר. הדחייה לא מחמירה עם הזמן — הוא ימתין.

כשתרצה לחדש, הפקודה הראשונה היא לבדוק אם יש חלופה לגלויה:

```
פתח את https://business.google.com והמשך את יצירת הפרופיל של Amora Studio.
אל תזין כתובת. בדוק אילו שיטות אימות מוצעות חוץ מגלויה — טלפון, הודעת טקסט,
מייל או סרטון. דווח לי מה מוצע בפועל ועצור. אם גלויה היא היחידה — אל תמשיך.
```

---

## 🧑 נדחה — מאסטר לוגו

ההגדלה נבדקה ואומתה (RMSE 3.49, האותיות זהות) אבל הפרוקסי של סביבת Claude
חוסם את ה-CDN. הקובץ:

```
https://d8j0ntlcm91z4.cloudfront.net/user_3GptPBn0PtejbdXxIAQaNLNkJaD/hf_20260804_231943_6c2236f1-4a9d-4f11-8e6f-36d7214ed09d.png
```

**איך להעביר את הקובץ אליי — נבדק ב-5.8, ורק דרך אחת עובדת:**

**לא** צירוף בצ'אט. ניסינו — התמונה מוצגת בשיחה אבל **אינה נוחתת על הדיסק**,
וחיפוש בכל מערכת הקבצים לא מצא כלום. `CLAUDE.md` טען את ההפך ותוקן.

**לא** הורדה ישירה. הפרוקסי מחזיר 403 ל-CloudFront, נבדק שוב באותו יום.

**כן — העלאה ל-GitHub:** `Add file` ← `Upload files` ← גרירה ← קומיט לענף.
`github.com` ברשימת ההיתר, ולכן אפשר למשוך משם מיד.

אחרי שהקובץ בריפו: הקטנה ל-512, החלפת `logo.jpg`, הרצה מחדש של
`tools/make-icons.mjs`, **והוספת אייקון 512 ל-`site.webmanifest`** — שהוא מה
שחסר היום כדי ש-Chrome יציע להתקין את האתר כאפליקציה.
